# 项目验收详细检查清单 (Detailed Checklist)

## 1. 核心功能验收 (Core Functionality)

### 1.1 用户认证与安全 (Auth & Security)
- [ ] **注册/登录**: 支持用户名/邮箱注册，密码强度检测（至少8位，含大小写字母和数字）。
- [ ] **JWT认证**: Token生成与验证正常，Token过期后自动刷新或强制登出。
- [ ] **双因素认证 (2FA)**:
    - [ ] 能够生成二维码供Google Authenticator绑定。
    - [ ] 登录时强制校验6位验证码。
    - [ ] 提供备用恢复码（Recovery Codes）。
- [ ] **单点登录 (SSO)**: (如适用) 验证OAuth2/OIDC集成流程。
- [ ] **会话管理**: 支持查看当前活跃会话，支持强制下线其他设备。

### 1.2 服务器实例管理 (Server Instance Mgmt)
- [ ] **添加服务器**: 支持通过 IP:Port (RCON/Query) 添加，自动验证连接有效性。
- [ ] **状态监控**:
    - [ ] 列表页实时刷新在线状态（在线/离线/无响应）。
    - [ ] 准确显示当前地图、游戏模式、在线人数/最大人数。
    - [ ] 延迟显示（Ping值）准确。
- [ ] **批量操作**: 支持选中多个服务器进行批量重启、关机或发送广播。
- [ ] **服务器分组**: 能按地区或用途创建分组，服务器归类正确。

### 1.3 实时游戏管理 (Live Game Mgmt)
- [ ] **实时数据推送**: WebSocket延迟 < 500ms，数据变更（如击杀、换图）前端立即响应。
- [ ] **地图管理**:
    - [ ] 显示当前地图轮换列表。
    - [ ] 支持立即更换地图（Change Map）。
    - [ ] 支持设置下一张地图（Set Next Map）。
- [ ] **对局控制**: 支持结束当前对局（End Game）、重置对局。

### 1.4 玩家管理 (Player Mgmt)
- [ ] **在线玩家列表**:
    - [ ] 显示SteamID、玩家名、所在队伍（Team ID）、小队（Squad）、击杀/死亡数（如RCON支持）。
    - [ ] 支持按名称或SteamID模糊搜索。
- [ ] **管理操作**:
    - [ ] **踢出 (Kick)**: 输入理由后踢出，验证玩家确实断开连接。
    - [ ] **封禁 (Ban)**: 支持设置封禁时长（临时/永久）和理由，验证BanList生效。
    - [ ] **警告 (Warn)**: 发送私聊消息给特定玩家。
- [ ] **历史记录**: 点击玩家能查看其历史游玩时长、违规记录。

### 1.5 配置文件管理 (Config Mgmt)
- [ ] **可视化编辑**:
    - [ ] `Server.cfg`: 常用参数（服务器名、最大人数）有表单控件支持。
    - [ ] `Admins.cfg`: 支持通过UI添加管理员SteamID和权限组。
    - [ ] `MapRotation.cfg`: 支持拖拽排序地图循环列表。
- [ ] **版本控制**: 每次保存生成版本快照，支持一键回滚到旧版本。
- [ ] **模板管理**: 能保存当前配置为模板，并应用到其他服务器。

### 1.6 日志与分析 (Logs & Analytics)
- [ ] **日志采集**: 能实时读取服务器日志文件（本地或远程）。
- [ ] **关键词高亮**: TeamKill、AdminCommand、Chat等关键事件高亮显示。
- [ ] **搜索与过滤**:
    - [ ] 支持按时间范围（如最近1小时）筛选。
    - [ ] 支持正则表达式搜索。
- [ ] **告警触发**: 当日志出现特定关键词（如 "Server Crash"）时触发系统告警。

### 1.7 RCON 命令终端 (RCON Terminal)
- [ ] **交互式终端**: 类似Shell的界面，输入命令后回车发送。
- [ ] **命令补全**: 输入部分命令（如 `Admin`）按Tab键提示补全。
- [ ] **历史记录**: 按上/下箭头键调用历史命令。
- [ ] **常用快捷键**: 界面提供 "广播"、"换图" 等快捷按钮。

## 2. 扩展功能验收 (Extended Features)

### 2.1 插件系统 (Plugin System)
- [ ] **插件加载**: 上传插件包后无需重启主程序即可加载（热加载或动态Import）。
- [ ] **生命周期**: 插件能响应 `onServerStart`, `onPlayerJoin` 等事件。
- [ ] **插件管理**: 启用/禁用插件功能正常，配置页面能读取插件定义的Schema。

### 2.2 任务调度 (Task Scheduling)
- [ ] **定时重启**: 设置每日/每周定时重启任务，任务准时触发。
- [ ] **定时广播**: 设置循环广播消息（如服务器规则），验证游戏内收到消息。

### 2.3 消息通知 (Notifications)
- [ ] **多渠道通知**: 验证邮件、Webhook（Discord/Slack）、短信通知能否发送成功。
- [ ] **触发规则**: 验证服务器崩溃、CPU超标（>90%）、VIP进入等事件触发通知。

### 2.4 高级游戏管理 (Advanced Game Mgmt)
- [ ] **智能监控**:
    - [ ] **卡假人**: 模拟挂机行为，验证是否被自动踢出。
    - [ ] **TPS告警**: 模拟服务器卡顿，验证前端是否显示TPS下降并告警。
    - [ ] **队伍名**: 创建违规队伍名，验证是否自动解散。
- [ ] **消息广播**:
    - [ ] **欢迎语**: 新玩家进入，验证屏幕显示个性化欢迎消息。
    - [ ] **黄字广播**: 发送 AdminBroadcast，验证游戏内显示醒目通知。
- [ ] **辅助功能**:
    - [ ] **打卡机**: 管理员在服时间统计准确，报表生成无误。
    - [ ] **快捷指令**: "拉研"、"重置载具" 等组合命令执行成功。

### 2.5 社区运营系统 (Community Ops)
- [ ] **积分系统**:
    - [ ] 游玩1小时，积分自动增加对应数值。
    - [ ] 积分排行榜显示正确。
- [ ] **CDK系统**:
    - [ ] 生成的CDK无法被重复使用。
    - [ ] 兑换成功后，积分或权限立即生效。
- [ ] **特殊豁免 (VIP)**:
    - [ ] 添加VIP后，`Admins.cfg` 或白名单文件自动更新。
    - [ ] VIP过期后自动移除权限。
- [ ] **团队管理**:
    - [ ] 能够创建战队，邀请成员。
    - [ ] 战队成员能够同步显示战队标签。

## 3. 非功能需求验收 (Non-Functional Requirements)

### 3.1 性能与稳定性 (Performance & Stability)
- [ ] **高并发**: 模拟管理50台服务器，前端页面不卡顿，后端CPU占用 < 50% (单核)。
- [ ] **内存泄漏**: 连续运行24小时，内存增长不超过初始值的20%。
- [ ] **断线重连**: RCON连接断开后，系统能自动尝试重连。

### 3.2 安全性 (Security)
- [ ] **数据加密**: 数据库中密码字段不可逆加密（Argon2/Bcrypt）。
- [ ] **操作审计**: 所有的敏感操作（Ban, Kick, 修改配置）均记录在审计日志中，包含操作人IP和时间。
- [ ] **权限隔离**: 普通管理员无法访问系统设置或插件管理页面。
- [ ] **API限流**: 对登录接口进行暴力破解测试，触发限流拦截。

### 3.3 兼容性与UI (Compatibility & UI)
- [ ] **响应式布局**:
    - [ ] 桌面端 (1920x1080) 显示完整。
    - [ ] 平板 (iPad) 触控操作正常。
    - [ ] 手机 (iPhone/Android) 侧边栏折叠，核心功能可用。
- [ ] **主题切换**: 深色/浅色模式切换无样式崩坏。
- [ ] **国际化**: 切换中/英文，界面文本（包括报错信息）全部替换。

## 4. 交付与文档验收 (Delivery & Documentation)

### 4.1 代码与构建 (Code & Build)
- [ ] **代码规范**: 通过 ESLint (JS/TS) 和 Prettier 检查。
- [ ] **单元测试**: 核心逻辑（Auth, RCON Parser）测试覆盖率 > 80%，所有用例通过。
- [ ] **构建产物**: 前端 Build 无报错，产物体积优化（Gzip后 < 1MB）。

### 4.2 文档资料 (Documentation)
- [ ] **API文档**: Swagger UI 可访问，接口描述清晰，包含示例请求/响应。
- [ ] **数据库文档**: ER图更新至最新，字段含义完整。
- [ ] **用户手册**: 包含详细的图文教程（如何添加服务器、如何配置2FA）。
- [ ] **开发文档**: 包含插件开发指南和SDK说明。

### 4.3 部署与运维 (Deployment & Ops)
- [ ] **Docker部署**: `docker-compose up -d` 能够拉起所有服务（App, DB, Redis）。
- [ ] **环境配置**: `.env.example` 文件完整，包含所有可配置项说明。
- [ ] **备份脚本**: 提供数据库自动备份脚本，并验证恢复流程有效。
- [ ] **HTTPS**: Nginx 配置模板包含 SSL 证书配置项。
